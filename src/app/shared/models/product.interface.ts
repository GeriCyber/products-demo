export interface Product {
    // TODO: remove
    categoria?: string;
    cod?: string;
    titulo?: string;
    url?: string;

    id?: string;
    name?: string;
    description?: string;
    longDescription?: string;
    image?: string;
    attempt?: boolean;
}
